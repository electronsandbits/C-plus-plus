/*
 * File: linearSearch.cc
 * ---------------------
 *  A linear search inspects elements in sequence
 *  until a match is found.
 */

#include <iostream>
using namespace std;


int main() {

   int pos = 0;
   bool found = false;

   while (pos < size of values  && !found)  {
      if (values[pos] == 100) {
        found = true; 
      }else {
        pos++;
      }
   }

}

